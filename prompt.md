# 宠物领养平台 UI 生成提示词

## 项目概述

这是一个基于 Next.js 16 + React 19 + Tailwind CSS 3 的宠物领养平台项目，使用 Supabase 作为后端服务。

**项目目标**: 连接爱心人士与需要家庭的宠物，创建一个温暖、友好的宠物领养平台。

## 技术栈

- **前端框架**: Next.js 16, React 19
- **样式方案**: Tailwind CSS 3.4
- **图标库**: Lucide React
- **后端服务**: Supabase (TypeScript)
- **类型定义**: TypeScript

## 功能模块清单

### 1. 首页 (`app/(main)/page.tsx`)
- **Hero Section**: 轮播图展示（3张）
- **快捷筛选**: 6个分类入口
  - 狗狗 🐕
  - 猫咪 🐱
  - 兔子 🐰
  - 鸟类 🐦
  - 其他 🐹
  - 查看全部 🔍
- **推荐宠物**: 4列卡片网格

### 2. 宠物列表页 (`app/(main)/pets/page.tsx`)
- **搜索栏**: 关键词搜索
- **筛选器**:
  - 品种
  - 年龄
  - 性别
  - 地区
- **排序**: 最新/最早/最多浏览
- **宠物卡片网格**: 1-4列响应式布局
- **加载状态**: 骨架屏

### 3. 宠物详情页 (`app/(main)/pets/[id]/page.tsx`)
- **图片轮播**: 主图 + 额外照片
- **基本信息**: 名称、品种、年龄、性别、位置、浏览量
- **发布者信息**: 发布者姓名、联系方式
- **领养申请表单**: 填写领养申请
- **操作按钮**: 返回列表、领养申请

### 4. 登录页 (`app/(auth)/login/page.tsx`)
- **登录表单**: 邮箱 + 密码
- **表单验证**: 邮箱格式、密码要求
- **错误提示**: 红色边框 + 错误消息
- **状态反馈**: 登录中 loading 状态

### 5. 注册页 (`app/(auth)/register/page.tsx`)
- **注册表单**:
  - 姓名
  - 邮箱
  - 密码（8位+，含大小写字母数字）
  - 确认密码
  - 手机号（可选）
  - 微信号（可选）
- **表单验证**: 实时验证
- **成功反馈**: 注册成功提示 + 前往登录按钮

### 6. 发布页面 (`app/(main)/publish/page.tsx`)
- **多步骤表单**:
  - 第1步：基本信息（名称、品种、年龄、性别、位置）
  - 第2步：照片上传（最多5张）
  - 第3步：详细描述
- **验证**: Zod schema 验证
- **步骤指示器**: 圆形编号显示

### 7. 领养申请页 (`app/(main)/applications/[id]/page.tsx`)
- **申请状态展示**: 等待审核中/审核通过/审核拒绝
- **申请详情**: 宠物信息、申请者信息、申请时间
- **操作按钮**: 返回、重新申请

### 8. 我的申请页 (`app/(main)/my-applications/page.tsx`)
- **申请列表**: 所有申请记录
- **状态筛选**: 全部/等待中/已通过/已拒绝
- **查看详情**: 跳转到申请详情页

## 设计系统规范

### 色彩系统

**Light Mode (默认)**
```css
--background: 0 0% 100%;              /* 纯白 */
--foreground: 24 12% 18%;             /* 深灰 */
--card: 0 0% 100%;                    /* 卡片背景 */
--card-foreground: 24 12% 18%;        /* 卡片文字 */
--primary: 18 93% 56%;                /* 珊瑚橙 #FF6B35 - 品牌色 */
--primary-foreground: 60 9.1% 97.8%;  /* 主色文字 */
--secondary: 180 47% 45%;             /* 青绿色 #4ECDC4 */
--secondary-foreground: 60 9.1% 97.8%;
--muted: 60 4.8% 95.9%;               /* 浅灰 */
--muted-foreground: 25 5.3% 44.7%;    /* 次要文字 */
--accent: 45 100% 70%;                /* 金黄色 */
--accent-foreground: 24 12% 18%;
--destructive: 351 82% 65%;           /* 红色 */
--destructive-foreground: 60 9.1% 97.8%;
--border: 214.3 31.8% 91.4%;          /* 边框 */
--input: 214.3 31.8% 91.4%;           /* 输入框 */
--ring: 18 93% 56%;                   /* Focus ring */
--radius: 1rem;                       /* 圆角 */
```

**Dark Mode**
```css
--background: 24 12% 10%;             /* 深灰黑 */
--foreground: 88 8% 92%;              /* 浅灰白 */
--card: 24 12% 12%;                   /* 卡片背景 */
--card-foreground: 88 8% 92%;         /* 卡片文字 */
--primary: 18 93% 65%;                /* 更亮的珊瑚色 */
--primary-foreground: 60 9.1% 97.8%;
--secondary: 180 47% 55%;             /* 青绿色 */
--secondary-foreground: 60 9.1% 97.8%;
--muted: 24 12% 18%;                  /* 边框色 */
--muted-foreground: 25 5.3% 65%;
--accent: 45 100% 75%;                /* 金黄色 */
--accent-foreground: 24 12% 18%;
--destructive: 351 82% 70%;           /* 更亮的红色 */
--destructive-foreground: 60 9.1% 97.8%;
--border: 24 12% 20%;                 /* 边框 */
--input: 24 12% 20%;
--ring: 18 93% 65%;
```

### 设计原则

1. **圆角设计**: 所有卡片和交互元素使用 `rounded-xl` (1rem)
2. **阴影层次**:
   - 基础: `shadow-sm`
   - Hover: `shadow-md` 或 `shadow-lg`
3. **动画效果**: 平滑过渡 `transition-all duration-200~300`
4. **间距系统**: 统一使用 4px/6px/8px 基础间距
5. **无障碍**: 最小点击区域 44px × 44px，支持键盘导航

## UI 组件规范

### Button 组件

**变体类型**
- `primary`: 主操作按钮（珊瑚橙色背景）
- `secondary`: 次要操作（青绿色背景）
- `outline`: 边框按钮
- `ghost`: 幽灵按钮（透明背景）
- `destructive`: 危险操作（红色背景）

**尺寸**
- `sm`: `h-10 px-3 text-sm`
- `md`: `h-11 px-4 py-2 text-base` (默认)
- `lg`: `h-12 px-6 text-lg`

**样式特点**
- 圆角 `rounded-xl`
- Hover 阴影 `hover:shadow-md`
- Focus ring `focus:ring-2`
- Disabled 状态 `opacity-50 cursor-not-allowed`

### Card 组件

**子组件结构**
- `Card`: 外层容器
- `CardHeader`: 顶部区域，带底部边框分隔
- `CardContent`: 内容区域
- `CardFooter`: 底部区域，带顶部边框分隔，浅灰背景

**样式**
- 背景: `bg-card`
- 边框: `border border-border`
- 阴影: `shadow-sm hover:shadow-lg`
- Hover 效果: `transition-all duration-300`

### PetCard 组件

**卡片结构**
```
┌─────────────────────────┐
│ [图片区域 224px 高度]    │
│  - 类型标签（右上角）     │
│  - 渐变遮罩（hover显示）  │
├─────────────────────────┤
│ [宠物名称]              │
│ 品种 • 年龄 • 性别       │
│ [描述文字，最多3行]      │
│ 位置图标 • 浏览量        │
└─────────────────────────┘
```

**类型标签颜色**
- 狗狗: 🐕 狗狗
- 猫咪: 🐱 猫咪

**状态标签颜色**（在详情页使用）
- `available`: 绿色背景
- `adopted`: 灰色背景
- `pending`: 黄色背景

**图片区域**
- 高度: `56` (224px)
- 图片缩放: `hover:scale-105 transition-transform duration-500`
- 渐变遮罩: `from-black/20 to-transparent opacity-0 group-hover:opacity-100`

### Input 组件

**样式**
- 圆角 `rounded-xl`
- Focus ring `ring-2`
- 输入错误状态：红色边框
- 错误消息：红色文字提示
- 最小高度: 44px（无障碍标准）

### Form 组件

**表单验证**
- 实时验证（blur 事件）
- 错误提示清晰可见
- 验证失败时显示错误信息

**多步骤表单**
- 步骤指示器：圆形编号
- 上一步/下一步按钮
- 进度条指示

### SearchBar 组件

**样式**
- 圆角 `rounded-xl`
- 左侧搜索图标
- placeholder 文字提示
- 占位符宽度固定

### FilterBar 组件

**筛选选项**
- 品种筛选
- 年龄筛选
- 性别筛选
- 地区筛选

**样式**
- 下拉菜单或标签选择
- 多选支持
- 重置按钮

### SortDropdown 组件

**排序选项**
- 最新发布
- 最早发布
- 最多浏览

### PetDetail 组件

**结构**
- 图片轮播（主图 + 额外照片）
- 基本信息卡片
- 发布者信息卡片
- 领养申请表单
- 返回按钮

## 页面布局规范

### Header 组件

**定位**: `sticky top-0 z-50`
**毛玻璃效果**: `backdrop-blur-md`
**响应式**: 移动端汉堡菜单
**导航项**:
- 首页
- 浏览宠物
- 领养流程

### Footer 组件

**布局**: 4 列栅格布局
**内容**:
- 品牌介绍
- 快速链接
- 支持帮助
- 联系方式
- 社交媒体图标

### 首页布局

1. **Hero Section**: 轮播图展示
2. **快捷筛选**: 6 个快捷入口（带 hover 动画）
3. **推荐宠物**: 4 列卡片网格
4. **底部信息**

### 宠物列表页布局

1. **搜索栏**: 带搜索图标和 placeholder
2. **筛选器**: 品种、年龄、性别、地区
3. **排序**: 下拉菜单
4. **卡片网格**: 1-4 列响应式布局
5. **加载状态**: 骨架屏

### 宠物详情页布局

1. **图片轮播**: 主图 + 额外照片
2. **基本信息卡片**:
   - 名称
   - 品种
   - 年龄
   - 性别
   - 位置
   - 浏览量
3. **发布者信息**:
   - 发布者姓名
   - 联系方式
4. **领养申请表单**
5. **返回按钮**

### 登录页布局

1. **卡片容器**: 圆角、阴影
2. **标题区域**: 欢迎回来、登录提示
3. **登录表单**:
   - 邮箱输入
   - 密码输入
   - 登录按钮
4. **底部链接**: 注册账号提示

### 注册页布局

1. **卡片容器**: 圆角、阴影
2. **标题区域**: 创建账户、注册提示
3. **注册表单**:
   - 姓名
   - 邮箱
   - 密码
   - 确认密码
   - 手机号（可选）
   - 微信号（可选）
   - 提交按钮
4. **成功状态**: 勾选图标 + 成功提示
5. **底部链接**: 登录提示

### 发布页布局

1. **多步骤表单**
2. **步骤指示器**
3. **各步骤内容**:
   - 第1步：基本信息
   - 第2步：照片上传
   - 第3步：详细描述

### 领养申请页布局

1. **状态展示卡片**
2. **申请详情**
3. **操作按钮**

### 我的申请页布局

1. **标题**
2. **状态筛选**
3. **申请列表**

## 文件结构

```
pet-adoption/
├── app/
│   ├── (main)/
│   │   ├── layout.tsx           # 主布局（Header + Footer）
│   │   ├── page.tsx              # 首页
│   │   ├── pets/
│   │   │   └── page.tsx          # 宠物列表页
│   │   ├── pets/
│   │   │   └── [id]/
│   │   │       └── page.tsx      # 宠物详情页
│   │   ├── publish/
│   │   │   └── page.tsx          # 发布页面
│   │   ├── applications/
│   │   │   └── [id]/
│   │   │       └── page.tsx      # 领养申请页
│   │   └── my-applications/
│   │       └── page.tsx          # 我的申请页
│   ├── (auth)/
│   │   ├── login/
│   │   │   └── page.tsx          # 登录页
│   │   └── register/
│   │       └── page.tsx          # 注册页
│   ├── layout.tsx                # 根布局
│   └── globals.css               # 全局样式和 CSS 变量
├── components/
│   ├── ui/
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   ├── Carousel.tsx
│   │   ├── Toast.tsx
│   │   └── ...
│   ├── pet/
│   │   ├── PetCard.tsx
│   │   ├── PetList.tsx
│   │   ├── PetForm.tsx
│   │   ├── PetDetail.tsx
│   │   ├── SearchBar.tsx
│   │   ├── FilterBar.tsx
│   │   └── SortDropdown.tsx
│   ├── auth/
│   │   ├── LoginForm.tsx
│   │   └── RegisterForm.tsx
│   ├── Header.tsx
│   └── Footer.tsx
└── types/
    └── supabase.ts               # 类型定义
```

## AI 任务要求

当被要求根据项目功能生成 UI 页面时，请：

### 1. 识别页面类型
- 根据请求识别需要生成的页面类型（首页、列表页、详情页、表单页等）
- 查看相关组件文件了解现有结构

### 2. 生成组件代码
- 创建或修改组件文件
- 确保符合设计系统规范（颜色、圆角、阴影、间距）
- 使用 Tailwind CSS 类名，避免内联样式

### 3. 遵循组件规范
- 使用 Button 组件时，根据用途选择合适的变体
- 使用 Card 组件时，正确使用 Header/Content/Footer 结构
- PetCard 组件需要包含图片、信息、操作按钮

### 4. 处理特殊功能
- 表单页面需要验证和错误提示
- 列表页面需要加载状态和空状态
- 详情页面需要轮播图和详细信息

### 5. 响应式设计
- 移动端优先设计
- 使用 Grid/Flexbox 实现响应式布局
- 在小屏幕上使用合适的断点

### 6. 确保一致性
- 所有颜色使用 CSS 变量
- 统一的圆角大小 (rounded-xl)
- 统一的阴影效果
- 统一的间距系统

### 7. 无障碍支持
- 最小点击区域 44px × 44px
- Focus ring 样式
- 适当的 ARIA 标签

## 关键参考信息

| 参数 | 值 |
|------|-----|
| 主色调 | 珊瑚橙 `#FF6B35` - 温暖、友好、充满爱心 |
| 圆角大小 | `1rem` (xl) |
| 最小点击区域 | `44px × 44px` |
| 过渡动画时长 | `200ms` (快速交互) / `300ms` (卡片悬停) |
| 表单验证 | Zod schema 验证 |

## 数据结构参考

### Pet (宠物类型)
```typescript
{
  id: string
  name: string
  breed: string
  age: number
  gender: 'male' | 'female' | 'unknown'
  location: string
  description: string
  status: 'available' | 'adopted' | 'pending'
  view_count: number
  created_at: Date
}
```

### PetForm (宠物发布表单)
```typescript
{
  // 第1步：基本信息
  name: string
  breed: string
  age: number
  gender: 'male' | 'female' | 'unknown'
  location: string

  // 第2步：照片上传
  photos: File[]

  // 第3步：详细描述
  description: string
}
```

### LoginForm (登录表单)
```typescript
{
  email: string
  password: string
}
```

### RegisterForm (注册表单)
```typescript
{
  name: string
  email: string
  password: string
  confirmPassword: string
  phone?: string
  wechat?: string
}
```

### Application (领养申请)
```typescript
{
  id: string
  pet_id: string
  user_id: string
  status: 'pending' | 'approved' | 'rejected'
  created_at: Date
  pet: Pet
  user: {
    id: string
    name: string
    email: string
    phone?: string
  }
}
```

## 常见页面模板

### 卡片网格页面模板
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
  {items.map((item) => (
    <Card key={item.id} className="overflow-hidden hover:shadow-xl transition-all">
      {/* 卡片内容 */}
    </Card>
  ))}
</div>
```

### 表单页面模板
```tsx
<div className="max-w-md mx-auto bg-white rounded-xl shadow-lg p-8">
  <h1 className="text-2xl font-bold mb-2">标题</h1>
  <p className="text-gray-600 mb-6">描述</p>
  <Form />
  <div className="text-center mt-6">
    <Link href="/other-page">链接文本</Link>
  </div>
</div>
```

### 加载状态模板
```tsx
<div className="flex justify-center items-center py-8">
  <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-primary"></div>
</div>
```
